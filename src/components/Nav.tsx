"use client";import Link from"next/link";import{usePathname}from"next/navigation";import{Home,Ticket,CalendarDays,History,FileInput,Sparkles,Settings}from"lucide-react";
const items=[['/',Home,'ホーム'],['/lotteries',Ticket,'抽選'],['/calendar',CalendarDays,'予定'],['/history',History,'履歴'],['/import',FileInput,'取込'],['/research',Sparkles,'調査'],['/settings',Settings,'設定']]as const;
export function Nav(){const path=usePathname();return <><aside className="sidebar"><Link href="/" className="brand">ポケ抽選メモ</Link><nav>{items.map(([href,Icon,label])=><Link key={href} href={href} className={path===href||href!=='/'&&path.startsWith(href)?'active':''}><Icon size={20}/><span>{label}</span></Link>)}</nav><small>非公式・ローカル保存</small></aside><nav className="bottom-nav" aria-label="メインナビゲーション">{items.slice(0,5).map(([href,Icon,label])=><Link key={href} href={href} aria-label={label} className={path===href?'active':''}><Icon size={21}/><span>{label}</span></Link>)}</nav></>}
