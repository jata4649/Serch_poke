import Dexie,{type EntityTable} from "dexie";import type{Product,Retailer,LotteryOffer,UserLotteryState,ImportBatch,Settings}from"@/types/models";
export class PokeDB extends Dexie{products!:EntityTable<Product,"id">;retailers!:EntityTable<Retailer,"id">;offers!:EntityTable<LotteryOffer,"id">;userStates!:EntityTable<UserLotteryState,"id">;importBatches!:EntityTable<ImportBatch,"id">;settings!:EntityTable<Settings,"id">;constructor(name="poke-lottery-memo"){super(name);this.version(1).stores({products:"id,normalizedName,name,releaseDate",retailers:"id,slug,name",offers:"id,&offerKey,productId,retailerId,applicationStart,applicationEnd,sourceType,updatedAt",userStates:"id,&offerId,status,updatedAt",importBatches:"id,status,importedAt",settings:"id"})}}
export const db=new PokeDB();
export const defaultSettings:Settings={id:"main",region:"全国",onlineOnly:false,hideApplied:true,showUnofficial:true,minConfidence:0,theme:"light"};
export async function upsertUserState(offerId:string,patch:Partial<UserLotteryState>){const old=await db.userStates.where("offerId").equals(offerId).first(),now=new Date().toISOString();const next:UserLotteryState={id:old?.id||`state_${crypto.randomUUID()}`,offerId,status:"not_applied",appliedAt:null,resultRecordedAt:null,purchasedAt:null,selectedStore:null,note:"",resultReminderEnabled:true,purchaseReminderEnabled:true,createdAt:old?.createdAt||now,updatedAt:now,...old,...patch};await db.userStates.put(next);return next}
